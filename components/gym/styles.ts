// Re-export de estilos para que las importaciones './styles' y '../styles' funcionen
// (antes este archivo estaba vacío y TypeScript resolvía aquí en vez de al directorio ./styles)
export { GymStyles } from './styles/GymStyles';
